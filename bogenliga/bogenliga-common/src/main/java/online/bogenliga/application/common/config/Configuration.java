package online.bogenliga.application.common.config;


/**
 * A tagging interface for configurations. A {@link Configuration} implementor
 * is usually a factory that produces the really wanted objects. So this interface
 * is no restriction, rather a marker for the (typsafe) configuration factories
 * and accessors.
 *
 * @author Thomas Wolf
 * @version 1.$Revision$
 */
public interface Configuration {
}
